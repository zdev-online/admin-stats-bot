module.exports = {
    // Токен группы с правами (Сообщения) и включенным longpool`ом (Сообщения)
    token: "",
    // ID Чата и ID назначения ( Узнать через команду /get и вставить сюда)
    // Заменить false на значения полученные из сообщения в беседе с ботом
    chatId: false,
    peerId: false,
    // Интервал отправки статистики (В минутах)
    time: 5
}